============
Installation
============

At the command line either via easy_install or pip::

    $ easy_install theblues
    $ pip install theblues

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv theblues
    $ pip install theblues